name: CI

on:

  pull_request:
    branches: ["master", "dev/*"]

  push:
    branches: ["master", "dev/*"]

jobs: 

  unit:
    uses: ./.github/workflows/unit.yml

  notebooks:
    uses: ./.github/workflows/notebook_smoke.yml
